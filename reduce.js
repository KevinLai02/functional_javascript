function countWords(inputWords) {
    return inputWords.reduce((accumulator, currentValue) => {
        box = accumulator.concat(currentValue)

        if(accumulator[currentValue])


        return box
    },[])
    
}

module.exports = countWords